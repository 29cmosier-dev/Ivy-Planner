# 🌿 Ivy-Planner Setup

To make the image upload feature work, you need to run the Python server. If you just open `index.html` in a browser, the "save" feature will not work!

## 1. First Time Setup (One time only)
Make sure you have Python installed, then open your terminal/command prompt in this folder and run:
```bash
pip install flask

